<% if current_user.role?(:customer) %>
	<%= render :partial => "order_list", locals: {status: 'All', orders: @all_orders} %>
<% else %>
	<%= render partial: "partials/admin_index_structure", locals: {model_name: "order", 
                                                         primary: "pending_orders", 
                                                         secondary: "past_orders",
                                                         sidebar: nil} %>
<% end %>